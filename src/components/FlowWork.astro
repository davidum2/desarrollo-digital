---

interface Props {
    
    pasos:{
        subtitulo:string
        titulo:string
        descripcion:string
    }[]
}

const {pasos} = Astro.props
---

<ol class="relative border-s border-light-accent1 dark:border-dark-accent1">                  
    {
        pasos.map((paso)=>(
    <li class="mb-10 ms-4">
        <div class="absolute w-3 h-3 bg-light-accent2 rounded-full mt-1.5 -start-1.5 border border-light-accent1 dark:border-gray-900 dark:bg-gray-700"></div>
        <time class="mb-1 text-sm font-normal leading-none text-light-accent2 dark:text-dark-accent2">{paso.subtitulo}</time>
        <h3 class="text-lg font-semibold text-gray-900 dark:text-white">{paso.titulo}</h3>
        <p class="mb-4 text-base font-normal text-gray-500 dark:text-gray-400">{paso.descripcion}</p>
        <slot/>
    <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M1 5h12m0 0L9 1m4 4L9 9"/>
  </svg></a>
    </li>
    ))}
    
</ol>

