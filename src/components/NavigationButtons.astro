<script>
  export let currentSection;
  export let totalSections;
  export let onNext;
  export let onPrevious;
</script>

<div class="navigation-buttons">
  <button onClick={onPrevious} disabled={currentSection === 0}>
    Anterior
  </button>
  <button onClick={onNext}>
    {currentSection === totalSections - 1 ? 'Enviar' : 'Siguiente'}
  </button>
</div>

<style>
  .navigation-buttons {
    display: flex;
    justify-content: space-between;
    margin-top: 20px;
  }
  button {
    padding: 10px 20px;
    font-size: 16px;
    cursor: pointer;
  }
  button:disabled {
    cursor: not-allowed;
    opacity: 0.5;
  }
</style>
